<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:minHeight="?android:attr/listPreferredItemHeight">

    <!-- First icon -->
    <ImageView
        android:id="@+id/header_img"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentStart="true"
        android:layout_centerVertical="true"
        android:layout_marginStart="@dimen/expandable_header_padding"
        android:layout_marginLeft="@dimen/expandable_header_padding"
        android:layout_marginEnd="@dimen/activity_horizontal_margin"/>
    <!-- Title -->
    <TextView
        android:id="@+id/header_title"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_toEndOf="@id/header_img"
        android:layout_centerVertical="true"
        android:textSize="@dimen/default_text_size"
        android:textColor="@color/colorPrimary"
        android:textStyle="bold"/>
    <!-- Arrow expand -->
    <ImageView
        android:id="@+id/header_expand"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentEnd="true"
        android:layout_centerVertical="true"
        android:layout_marginEnd="@dimen/activity_horizontal_margin" />
    <View
        android:layout_width="match_parent"
        android:layout_height="1dp"
        android:background="@color/colorAccent"
        android:layout_alignParentBottom="true"
        android:layout_marginStart="@dimen/expandable_item_padding"
        android:layout_marginEnd="@dimen/expandable_item_padding"/>

</RelativeLayout>